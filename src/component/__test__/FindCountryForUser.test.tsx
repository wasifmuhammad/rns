import React from "react";
import { ReactDOM } from "react";
import FindCountryForUser from "../FindCountryForUser";

import { render } from "@testing-library/react";
it("renders without crashing", () => {
  // stuck at some bug, can't write any test at this point
});
it("render component correctl", () => {
  //
});
